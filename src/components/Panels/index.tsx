import { Box } from "@mui/system";

const Panels = () => {
  return (
    <Box
      component="div"
      sx={{
        display: "flex",
        flexGrow: 1,
        height: "100%",
        maxWidth: "100%",
        border: "10px solid black;",
        gap: "0.5rem",
        padding: "0.4rem",
        overflowX: "scroll",
      }}
    >

      
    </Box>
  );
};

export default Panels;
